{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main-window",
  "description": "Minimal core capability for the DOM Wallet V3 main window.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
